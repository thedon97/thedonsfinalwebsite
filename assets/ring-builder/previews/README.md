Place final center-preview renders here.

Fallback lookup order:
1. `{shape}--{setting}--{metal}--{head}--{band}.webp`
2. `{shape}--{setting}--{metal}.webp`
3. `{shape}--{setting}.webp`
4. `{shape}.webp`
5. `default-luxury-ring.svg`

Use original generated assets, licensed stock, supplier/manufacturer-approved images, or owned CAD renders only.
